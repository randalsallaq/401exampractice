<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% result.forEach(element=>{ %>
        <div>
        <p> Joke Number: <%= element.id %></p>
        <p> type: <%= element.type %></p>
        <p> setup: <%= element.setup %></p>
        <p> punchline: <%= element.punchline %></p>


        <a href="/joke/<%= element.id %>">view details</a>
    </div>
    <% }); %>
</body>
</html>